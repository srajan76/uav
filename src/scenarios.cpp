#include "scenarios.hpp"

Scenarios::Scenarios() :
    _numTargets(), 
    _numScenarios(2000),
    _maxBatches(10),
    _maxBatchSize(100), 
    _omega() {};

void Scenarios::generateScenarios() {

    return;
};

std::vector<std::vector<int>> getScenarios(
    int batchId, int scenariosPerBatch) {
    
    std::vector<std::vector<int>> scenarios{{0}};

    return scenarios;
};